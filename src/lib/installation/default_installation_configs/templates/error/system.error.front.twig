<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Error - {{ throwable.getMessage }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
            Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
            background: #f5f5f5;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .error-container {
            margin: auto;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .error-header {
            background: #b71c1c;
            color: #fff;
            padding: 15px 20px;
            font-size: 20px;
        }
        .error-details {
            padding: 20px;
            border-bottom: 1px solid #eee;
        }
        .error-stack {
            padding: 20px;
            background: #fafafa;
            font-family: monospace;
            white-space: pre-wrap;
            overflow-x: auto;
        }
    </style>
</head>
<body>
<div class="error-container">
    <div class="error-header">
        {{ throwable.getMessage }}
    </div>
    <div class="error-details">
        <strong>Type:</strong> {{ throwable|class }}<br>
        <strong>File:</strong> {{ throwable.getFile }}<br>
        <strong>Line:</strong> {{ throwable.getLine }}<br>
    </div>
    <div class="error-stack">
        {{ throwable.getTraceAsString }}
    </div>
</div>
</body>
</html>
